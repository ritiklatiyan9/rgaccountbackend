-- Accounting table columns and CHECK constraints verified from read-only metadata on 2026-09-11.

-- No customer records. Foreign-key parents are supplied by the isolated test.

CREATE TABLE cash_flow_entries (
  id SERIAL PRIMARY KEY,
  cash_flow_month_id integer NOT NULL,
  site_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  particular character varying(500) NOT NULL,
  debit numeric(15,2) DEFAULT 0 NOT NULL,
  credit numeric(15,2) DEFAULT 0 NOT NULL,
  remarks text,
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  cash_type character varying(20) DEFAULT 'bank'::character varying NOT NULL,
  voucher_url character varying(1000),
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  is_firm_transaction boolean DEFAULT false NOT NULL,
  from_firm_id integer,
  to_firm_id integer,
  to_name character varying(255),
  source_module character varying(50),
  source_id integer,
  assigned_admin_id integer,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  customer_signature_url text,
  authority_signature_url text,
  bank_account_id integer,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT cash_flow_entries_cash_type_check CHECK (((cash_type)::text = ANY ((ARRAY['cash'::character varying, 'bank'::character varying, 'cheque'::character varying])::text[]))),
  CONSTRAINT cash_flow_entries_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(cash_type, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT cash_flow_entries_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  UNIQUE(source_module,source_id)
);

CREATE TABLE cash_flow_months (
  id SERIAL PRIMARY KEY,
  site_id integer NOT NULL,
  month integer NOT NULL,
  year integer NOT NULL,
  opening_balance numeric(15,2) DEFAULT 0 NOT NULL,
  notes text,
  is_locked boolean DEFAULT false,
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  ledger_name character varying(255),
  ledger_type character varying(20) DEFAULT 'site'::character varying,
  linked_user_id integer,
  linked_member_id integer,
  CONSTRAINT cash_flow_months_ledger_type_check CHECK (((ledger_type)::text = ANY ((ARRAY['site'::character varying, 'person'::character varying])::text[]))),
  CONSTRAINT cash_flow_months_month_check CHECK (((month >= 1) AND (month <= 12))),
  UNIQUE(site_id,month,year,ledger_name)
);

CREATE TABLE day_book (
  id SERIAL PRIMARY KEY,
  site_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  particular character varying(500) NOT NULL,
  entry_type character varying(50) DEFAULT 'GENERAL'::character varying NOT NULL,
  debit numeric(15,2) DEFAULT 0 NOT NULL,
  credit numeric(15,2) DEFAULT 0 NOT NULL,
  remarks text,
  payment_mode character varying(50),
  category character varying(100),
  from_entity character varying(255),
  to_entity character varying(255),
  account_no character varying(100),
  branch character varying(255),
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  farmer_payment_id integer,
  commission_id integer,
  cash_flow_entry_id integer,
  firm_transaction_id integer,
  plot_payment_id integer,
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  imprest_allocation_id integer,
  assigned_user_id integer,
  voucher_url character varying(1000),
  vendor_payment_id integer,
  assigned_admin_id integer,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  is_imprest_internal boolean DEFAULT false NOT NULL,
  is_financial_projection boolean DEFAULT false NOT NULL,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT day_book_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT day_book_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT day_book_entry_type_check CHECK (((entry_type)::text = ANY ((ARRAY['GENERAL'::character varying, 'EXPENSE'::character varying, 'INCOME'::character varying, 'PAYMENT'::character varying, 'RECEIPT'::character varying, 'TRANSFER'::character varying, 'ADJUSTMENT'::character varying, 'OTHER'::character varying, 'FARMER PAYMENT'::character varying, 'PLOT COMMISSION'::character varying, 'CASH FLOW'::character varying, 'FIRM TRANSACTION'::character varying, 'PLOT PAYMENT'::character varying, 'IMPREST'::character varying, 'VENDOR PAYMENT'::character varying])::text[]))),
  CONSTRAINT day_book_imprest_internal_type_check CHECK (((NOT is_imprest_internal) OR (upper((COALESCE(entry_type, ''::character varying))::text) = 'IMPREST'::text))),
  CONSTRAINT day_book_status_check CHECK (((status)::text = ANY ((ARRAY['pending'::character varying, 'approved'::character varying, 'rejected'::character varying])::text[])))
);

CREATE TABLE expenses (
  id SERIAL PRIMARY KEY,
  site_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  from_entity character varying(255),
  to_entity character varying(255),
  payment_mode character varying(50) DEFAULT 'CASH'::character varying,
  debit numeric(15,2) DEFAULT 0 NOT NULL,
  credit numeric(15,2) DEFAULT 0 NOT NULL,
  remark text,
  account_no character varying(100),
  branch character varying(255),
  category character varying(100),
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_user_id integer,
  voucher_url character varying(1000),
  assigned_admin_id integer,
  bill_url text,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  display_order integer,
  voucher_urls TEXT[],
  bill_urls TEXT[],
  imprest_proof_key text,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  sub_category character varying(100),
  CONSTRAINT expenses_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT expenses_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT expenses_status_check CHECK (((status)::text = ANY ((ARRAY['pending'::character varying, 'approved'::character varying, 'rejected'::character varying, 'waiting'::character varying, 'returned'::character varying])::text[])))
);

CREATE TABLE farmer_payments (
  id SERIAL PRIMARY KEY,
  farmer_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  particular character varying(255) NOT NULL,
  amount numeric(15,2) DEFAULT 0 NOT NULL,
  by_note character varying(500),
  interest_rate numeric(5,2) DEFAULT 0,
  interest_amount numeric(15,2) DEFAULT 0,
  remarks text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  payment_mode character varying(20) DEFAULT 'CASH'::character varying,
  cash_amount numeric(15,2) DEFAULT 0,
  bank_amount numeric(15,2) DEFAULT 0,
  bank_name character varying(255),
  bank_account_no character varying(100),
  bank_reference character varying(255),
  bank_ifsc character varying(20),
  voucher_url character varying(1000),
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  created_by integer,
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT farmer_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT farmer_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[]))))
);

CREATE TABLE land_deal_payments (
  id SERIAL PRIMARY KEY,
  land_deal_id integer NOT NULL,
  site_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  amount numeric(15,2) NOT NULL,
  payment_mode character varying(20) DEFAULT 'CASH'::character varying NOT NULL,
  bank_name character varying(150),
  bank_account_no character varying(50),
  bank_reference character varying(120),
  bank_ifsc character varying(20),
  cheque_no character varying(50),
  cheque_status character varying(20),
  remarks text,
  voucher_url text,
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  customer_signature_url text,
  authority_signature_url text,
  created_by integer,
  created_at timestamp with time zone DEFAULT now() NOT NULL,
  updated_at timestamp with time zone DEFAULT now() NOT NULL,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT land_deal_payments_amount_check CHECK ((amount > (0)::numeric)),
  CONSTRAINT land_deal_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT land_deal_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[]))))
);

CREATE TABLE misc_income_entries (
  id SERIAL PRIMARY KEY,
  site_id integer NOT NULL,
  category_id integer NOT NULL,
  direction character varying(6) DEFAULT 'credit'::character varying NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  amount numeric(15,2) NOT NULL,
  payment_mode character varying(20) DEFAULT 'CASH'::character varying NOT NULL,
  party_name character varying(255),
  bank_name character varying(150),
  bank_account_no character varying(50),
  bank_reference character varying(120),
  bank_ifsc character varying(20),
  cheque_no character varying(50),
  cheque_status character varying(20),
  remarks text,
  voucher_url text,
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  created_by integer,
  created_at timestamp with time zone DEFAULT now() NOT NULL,
  updated_at timestamp with time zone DEFAULT now() NOT NULL,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT misc_income_entries_amount_check CHECK ((amount > (0)::numeric)),
  CONSTRAINT misc_income_entries_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT misc_income_entries_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT misc_income_entries_direction_check CHECK (((direction)::text = ANY ((ARRAY['credit'::character varying, 'debit'::character varying])::text[])))
);

CREATE TABLE plot_commission_payments (
  id SERIAL PRIMARY KEY,
  site_id integer,
  plot_commission_id integer,
  date date DEFAULT CURRENT_DATE NOT NULL,
  amount numeric(12,2) DEFAULT 0 NOT NULL,
  balance_after_payment numeric(12,2) DEFAULT 0 NOT NULL,
  payment_mode character varying(20) DEFAULT 'CASH'::character varying,
  bank_name character varying(100),
  transaction_id character varying(100),
  remarks text,
  status character varying(20) DEFAULT 'pending'::character varying,
  voucher_number character varying(50),
  voucher_url text,
  created_by integer,
  approved_by integer,
  approved_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  assigned_admin_id integer,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT plot_commission_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT plot_commission_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[]))))
);

CREATE TABLE plot_payments (
  id SERIAL PRIMARY KEY,
  plot_id integer NOT NULL,
  site_id integer NOT NULL,
  date date DEFAULT CURRENT_DATE NOT NULL,
  payment_from character varying(100),
  bank_details character varying(255),
  narration text,
  received_by character varying(255),
  amount numeric(15,2) DEFAULT 0 NOT NULL,
  created_by integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  payment_type character varying(20) DEFAULT 'CASH'::character varying,
  voucher_url character varying(1000),
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  bank_name character varying(150),
  branch character varying(150),
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  buyer_name character varying(255),
  booked_by character varying(255),
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  money_transfer_id uuid,
  money_transfer_role text,
  CONSTRAINT plot_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_type, ''::character varying))) <> 'CHEQUE'::text) OR (cheque_status IS NOT NULL))),
  CONSTRAINT plot_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT plot_payments_money_transfer_role_check CHECK ((money_transfer_role = ANY (ARRAY['debit'::text, 'credit'::text]))),
  CONSTRAINT plot_payments_payment_type_check CHECK (((payment_type)::text = ANY ((ARRAY['CASH'::character varying, 'BANK'::character varying, 'CHEQUE'::character varying])::text[])))
);

CREATE TABLE vendor_inventory_orders (
  id SERIAL PRIMARY KEY,
  site_id integer NOT NULL,
  vendor_member_id integer,
  vendor_name character varying(200) NOT NULL,
  item_name character varying(200) NOT NULL,
  item_category character varying(120),
  unit character varying(40) DEFAULT 'pcs'::character varying NOT NULL,
  qty_ordered numeric(14,3) DEFAULT 0 NOT NULL,
  qty_received numeric(14,3) DEFAULT 0 NOT NULL,
  rate numeric(14,4) DEFAULT 0 NOT NULL,
  discount_pct numeric(6,3) DEFAULT 0 NOT NULL,
  discount_amount numeric(14,2) DEFAULT 0 NOT NULL,
  gross_amount numeric(14,2),
  net_amount numeric(14,2),
  total_paid numeric(14,2) DEFAULT 0 NOT NULL,
  order_date date NOT NULL,
  expected_date date,
  note text,
  status character varying(20) DEFAULT 'open'::character varying NOT NULL,
  created_by integer,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  commitment_id integer,
  CONSTRAINT vendor_inventory_orders_discount_amount_check CHECK ((discount_amount >= (0)::numeric)),
  CONSTRAINT vendor_inventory_orders_discount_pct_check CHECK (((discount_pct >= (0)::numeric) AND (discount_pct <= (100)::numeric))),
  CONSTRAINT vendor_inventory_orders_qty_ordered_check CHECK ((qty_ordered >= (0)::numeric)),
  CONSTRAINT vendor_inventory_orders_qty_received_check CHECK ((qty_received >= (0)::numeric)),
  CONSTRAINT vendor_inventory_orders_rate_check CHECK ((rate >= (0)::numeric)),
  CONSTRAINT vendor_inventory_orders_status_check CHECK (((status)::text = ANY ((ARRAY['open'::character varying, 'partial'::character varying, 'completed'::character varying, 'cancelled'::character varying])::text[])))
);

CREATE TABLE vendor_inventory_payments (
  id SERIAL PRIMARY KEY,
  order_id integer NOT NULL,
  site_id integer NOT NULL,
  payment_date date NOT NULL,
  amount numeric(14,2) NOT NULL,
  payment_mode character varying(20) DEFAULT 'cash'::character varying NOT NULL,
  reference_no character varying(120),
  cheque_no character varying(50),
  note text,
  voucher_url text,
  created_by integer,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  cheque_status character varying(20),
  source_vendor_payment_id integer,
  updated_at timestamp with time zone DEFAULT now(),
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT vendor_inventory_payments_amount_check CHECK ((amount > (0)::numeric)),
  CONSTRAINT vendor_inventory_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT vendor_inventory_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT vendor_inventory_payments_payment_mode_check CHECK (((payment_mode)::text = ANY ((ARRAY['cash'::character varying, 'bank'::character varying, 'upi'::character varying, 'cheque'::character varying, 'neft'::character varying, 'rtgs'::character varying, 'imps'::character varying, 'other'::character varying])::text[])))
);

CREATE TABLE vendor_payments (
  id SERIAL PRIMARY KEY,
  commitment_id integer NOT NULL,
  site_id integer NOT NULL,
  payment_date date NOT NULL,
  amount numeric(14,2) NOT NULL,
  payment_mode character varying(20) DEFAULT 'cash'::character varying NOT NULL,
  reference_no character varying(120),
  note text,
  voucher_url text,
  created_by integer,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  status character varying(20) DEFAULT 'pending'::character varying NOT NULL,
  approved_by integer,
  approved_at timestamp with time zone,
  assigned_admin_id integer,
  cheque_status character varying(20) DEFAULT NULL::character varying,
  cheque_no character varying(50) DEFAULT NULL::character varying,
  customer_signature_url text,
  authority_signature_url text,
  mapped_member_id integer,
  mapped_user_id integer,
  updated_at timestamp with time zone DEFAULT now(),
  cheque_status_updated_at timestamp with time zone,
  transaction_time time without time zone DEFAULT ((CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text))::time(0) without time zone,
  CONSTRAINT vendor_payments_amount_check CHECK ((amount > (0)::numeric)),
  CONSTRAINT vendor_payments_cheque_requires_status CHECK (((upper(TRIM(BOTH FROM COALESCE(payment_mode, ''::character varying))) <> ALL (ARRAY['CHEQUE'::text, 'CHECK'::text])) OR (cheque_status IS NOT NULL))),
  CONSTRAINT vendor_payments_cheque_status_valid CHECK (((cheque_status IS NULL) OR ((cheque_status)::text = ANY ((ARRAY['PENDING'::character varying, 'CLEARED'::character varying, 'BOUNCED'::character varying, 'RETURNED'::character varying])::text[])))),
  CONSTRAINT vendor_payments_payment_mode_check CHECK (((payment_mode)::text = ANY ((ARRAY['cash'::character varying, 'bank'::character varying, 'upi'::character varying, 'cheque'::character varying, 'neft'::character varying, 'rtgs'::character varying, 'imps'::character varying, 'other'::character varying])::text[]))),
  CONSTRAINT vendor_payments_status_check CHECK (((status)::text = ANY ((ARRAY['pending'::character varying, 'approved'::character varying, 'rejected'::character varying])::text[])))
);
